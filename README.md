Terraform deployment inside of Azure

Deploys a VNet, subnet, NSG, Key Vault, policy assignment, and Defender for Cloud

Currently the terraform layout includes the root directory which has files, main.tf, outputs.tf, and variables.tf. 
There is a also a modules folder that includes a network subfolder that includes networking related azure changes.



